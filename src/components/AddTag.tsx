import {
  Collapse,
  Drawer,
  DrawerBody,
  DrawerContent,
  // DrawerHeader,
  DrawerOverlay,
  useDisclosure,
} from "@chakra-ui/react";

const AddTag = () => {
  const { isOpen, onOpen, onClose } = useDisclosure();

  return (
    <></>
  );
};
export default AddTag;
